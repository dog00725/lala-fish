<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo" xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd
        ">

    <!-- 加解密服务 -->
    <dubbo:reference id="loanAESCryptService"
                     interface="com.lianlian.loan.lianlian.common.crypt.service.ILoanAESCryptService" timeout="${dubbo.consumer.timeout}"
                     check="${dubbo.consumer.check}" loadbalance="${dubbo.consumer.loadbalance}"/>

    <dubbo:reference id="loanSerialService"
                     interface="com.lianlian.loan.lianlian.common.idgen.service.ILoanSerialService"
                     timeout="${dubbo.consumer.timeout}"
                     check="${dubbo.consumer.check}" loadbalance="${dubbo.consumer.loadbalance}"/>

</beans>